{
  "name": "basic",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build",
    "preview": "wrangler dev",
    "preview:remote": "wrangler dev --env production --remote",
    "deploy": "npm run build && wrangler deploy --env production",
    "test": "vitest"
  },
  "private": true,
  "dependencies": {
    "hono": "^4.11.3",
    "honox": "0.1.52"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.1",
    "@cloudflare/workers-types": "^4.20250214.0",
    "@hono/vite-build": "^1.3.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@tailwindcss/vite": "^4.0.9",
    "tailwindcss": "^4.0.9",
    "vite": "^6.3.5",
    "vitest": "3.2",
    "wrangler": "^4.4.0"
  }
}
